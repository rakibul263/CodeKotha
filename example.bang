# BangLang উদাহরণ প্রোগ্রাম
# ========================

# ভেরিয়েবল ডিক্লেয়ারেশন
পূর্ণসংখ্যা x = ১০;
পূর্ণসংখ্যা y = ৫;

# গাণিতিক অপারেশন
দেখাও(x + y);    # যোগ: 15
দেখাও(x - y);    # বিয়োগ: 5
দেখাও(x * y);    # গুণ: 50
দেখাও(x / y);    # ভাগ: 2

# তুলনা অপারেশন
দেখাও(x > y);    # বড়: 1 (true)
দেখাও(x < y);    # ছোট: 0 (false)

# কন্ডিশনাল স্টেটমেন্ট
যদি (x > y) {
    দেখাও(x);
} নইলে {
    দেখাও(y);
}

# একাধিক প্যারামিটার প্রিন্ট
দেখাও(x, y); 